import { Type } from '@nestjs/common';
import { NestContainer } from './container';
import { Module } from './module';
export declare class ContainerScanner {
    private readonly container;
    private flatContainer;
    constructor(container: NestContainer);
    find<TInput = any, TResult = TInput>(typeOrToken: Type<TInput> | string | symbol): TResult;
    findInstanceByPrototypeOrToken<TInput = any, TResult = TInput>(metatypeOrToken: Type<TInput> | string | symbol, contextModule: Partial<Module>): TResult;
    private initFlatContainer();
}
